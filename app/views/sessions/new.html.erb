<h1>Login</h1>

<%= form_with model: @user, local: true, url: sessions_path, method: 'post' do |f| %>
  <%= f.label 'Username' %>
  <%= f.text_field :username, value: @user.username %>

  <%= f.label 'Password' %>
  <%= f.password_field :password %>

  <%= f.submit 'Log in' %>
<% end %>
